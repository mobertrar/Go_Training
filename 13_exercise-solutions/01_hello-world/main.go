package main

import "fmt"

func main() {
	fmt.Println([]byte("Hello World"))
}

//conrol flow is the sequence, loop (iterations), and conditionals

//[]byte() is a conversion or slice of byte to see the ASCII unicode rune that represents the numeric decimal code points
